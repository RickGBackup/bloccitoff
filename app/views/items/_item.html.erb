<li id="js-item-<%= item.id %>"><%= item.name %>
  <% if item.user == current_user %>
    <%= link_to "", [item.user, item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %>
  <% end %>  
  <em><%= time_remaining(item) %></em>
</li>
